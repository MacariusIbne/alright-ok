function changeColor(color) {
  document.getElementById("colorBox").style.backgroundColor = color;
} // so by pressing it, the larger button will turn into the colour that was pressed or selected on
// so like if you press red, the larger box will turn red
